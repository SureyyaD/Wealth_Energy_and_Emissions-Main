# Auto-detect text files and normalize them in the repository.
* text=auto

# Docker-related files must stay LF for Unix-based tooling.
*.yml          text eol=lf
*.yaml         text eol=lf
*Dockerfile*   text eol=lf
*.sh           text eol=lf
*.env          text eol=lf
*.env.*        text eol=lf
